import { ConfirmPassword.Validator } from './confirm-password.validator';

describe('ConfirmPassword.Validator', () => {
  it('should create an instance', () => {
    expect(new ConfirmPassword.Validator()).toBeTruthy();
  });
});
